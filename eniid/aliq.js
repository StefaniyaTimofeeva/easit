if (globalThis.Bun) {
    console.log('Bun exists!');
    // Additional code that runs if Bun is truthy
} else {
    console.log('Bun does not exist or is falsy.');
    // Additional code that runs if Bun is falsy
}
